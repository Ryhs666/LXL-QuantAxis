# LXL·QuantAxis v5.0 使用手册

## 启动方式

双击桌面 **`QuantAxis量化系统.bat`** 打开桌面应用，或双击 **`LXL·QuantAxis.bat`** 打开 Web 平台。

---

## 一、每日开盘（仪表盘）

打开即看：

| 板块 | 内容 | 作用 |
|------|------|------|
| 我的持仓 | 每只持仓股的成本、现价、盈亏%、健康评分 | 一眼看出哪只该留、哪只该走 |
| 今日强势股 | 13只默认标的的因子评分排名 | 自动筛选买入信号最强的股票 |
| K线图 | TradingView 专业K线 | 可视化行情走势 |
| 快速回测 | 选股票+策略→立刻看历史表现 | 验证你的交易想法 |

---

## 二、交易实战（核心功能）

### 快速验证
1. 点 `🔍 快速验证`
2. 输入股票代码（如 `600498`）→ 系统自动补全名称
3. 下拉选择策略（如 `双均线交叉`）
4. 选日期 → 点 `运行验证`
5. 看结果：收益率、夏普比率、最大回撤、交易记录

### 个股诊断
1. 点 `🩺 个股诊断`
2. 输入股票代码 → 勾选要测试的策略（默认全选）
3. 点 `开始诊断`
4. 输出：全部策略排名表 + 当前入场时机评分

### 智能推荐 ⭐
1. 点 `💡 智能推荐`
2. 输入股票代码 → 点 `获取推荐`
3. 系统自动：
   - 跑全部 15 个策略 → 找最优
   - 计算 18 个因子 → 判趋势
   - 给出：**具体买入价、卖出价、止损价、建议仓位**

### 因子策略构建器
1. 点 `🧬 因子策略`
2. 勾选你要用的因子（如 RSI + 布林 + 成交量）
3. 设定每个因子的条件（lt=小于/gt=大于）、阈值、权重
4. 选逻辑模式（weighted/and/or）
5. 点 `构建并回测` → 立即看这个因子组合的历史表现

### 每日快扫
1. 点 `🔄 每日快扫`
2. 选快速扫描（因子评分）或完整诊断（含策略回测）
3. 自动刷新行情 → 13只标的按评分排名 → 信号汇总（买入/观望/回避）

---

## 三、策略与因子

### 策略列表
查看全部 11 个经典策略 + 4 个因子策略 = 15 个策略的详细信息和参数范围。

### 因子体系
查看全部 18 个因子的名称、分类、描述。

---

## 四、AI 智能体

使用前需配置 AI 密钥（DeepSeek/OpenAI 兼容）：
- **桌面应用**：左侧 `AI智能体` → `配置AI`
- 输入 API Key、Base URL、Model → 保存

| 功能 | 作用 |
|------|------|
| AI 对话 | 和 AI 聊量化，可语音操控（说"跑回测 600498"） |
| 策略工厂 | AI + 遗传算法自动进化策略，找到最优参数 |
| 策略银行 | 保存和管理你的策略库 |
| AI 复盘 | 分析你的交易记录，找出行为偏差和改进方向 |
| AI 市场简报 | AI 生成今日市场概况 |

---

## 五、分析复盘

| 功能 | 作用 |
|------|------|
| 绩效分析报告 | 按市场/策略/月份分组查看交易盈亏 |
| 因子体系 | 浏览全部 18 个因子 |

---

## 六、命令行快速指令（桌面应用右侧控制台）

在底部输入框输入：

| 命令 | 作用 |
|------|------|
| `help` | 查看所有命令 |
| `status` | 系统状态摘要 |
| `backtest 600498` | 快速回测 |
| `batch` | 批量回测 |
| `valuation` | 指数估值快照 |
| `clear` | 清空控制台 |

---

## 七、典型使用场景

### 场景1：早上开盘，想看今天该不该买
1. 打开软件 → 仪表盘自动显示"今日强势股"
2. 看到评分最高的股票 → 点 `智能推荐`
3. 输入代码 → 获得具体买入价、止损价
4. 用给的价位下单

### 场景2：持仓股跌了，不知道该不该卖
1. 仪表盘 → "我的持仓" → 看健康评分
2. 如果健康分 < 20 → 系统已标注"建议离场"
3. 可再点 `个股诊断` 看这只股票的 15 策略排名和入场评分
4. 根据 AI 复盘的建议调整

### 场景3：想验证一个交易想法
1. 点 `因子策略构建器`
2. 选择你认为有效的因子组合
3. 直接回测看历史表现
4. 如果夏普 > 0.5 → 可用于实盘

### 场景4：盘后复盘
1. 点 `AI复盘 & 顾问`
2. AI 自动分析你的交易记录
3. 找出：追涨杀跌、止损不严、仓位过重等问题
4. 按 AI 建议改进下一笔交易

---

## 八、策略说明

| 策略 | 适用场景 | 特点 |
|------|----------|------|
| 双均线交叉 | 趋势市场 | 金叉买、死叉卖 |
| RSI超买超卖 | 震荡市场 | RSI<30买、>70卖 |
| MACD金叉死叉 | 趋势市场 | 零轴上方金叉更可靠 |
| 布林带 | 震荡市场 | 下轨买、上轨卖 |
| 海龟交易 | 强趋势 | 突破N日高点入场 |
| 均值回归 | 震荡市场 | 远离均线逆势入场 |
| 动量突破 | 趋势市场 | 突破高点+成交量确认 |
| 自适应复合 | 所有市场 | 自动检测状态切换策略 |
| 双向交易 | 下跌市场 | 做空获利 |
| 状态感知 | 所有市场 | 5状态分类自适应 |
